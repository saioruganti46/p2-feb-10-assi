class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        d={}
        for i in l:
            if i not ind:
                d[i]=1
            else:
                d[i]=d[i]+1
        for i in d:
            if d[i]==1:
                return ind            
